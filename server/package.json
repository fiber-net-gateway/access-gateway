{
  "name": "@access-gateway/server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json --pretty false",
    "test": "node --import tsx --test src/app.test.ts src/config/env.test.ts"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "fastify": "^5.11.3"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "tsx": "^4.23.11",
    "typescript": "^7.0.2"
  }
}
