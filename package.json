{
  "name": "access-gateway",
  "private": true,
  "version": "0.1.0",
  "description": "Access Gateway data plane and management console",
  "workspaces": [
    "web",
    "server"
  ],
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "concurrently --kill-others-on-fail -n web,server -c cyan,magenta \"npm run dev:web\" \"npm run dev:server\"",
    "dev:web": "npm run dev --workspace web",
    "dev:server": "npm run dev --workspace server",
    "db:migrate": "npm run db:migrate --workspace server",
    "docs:build": "node scripts/build-user-guide.mjs",
    "docs:check": "node scripts/build-user-guide.mjs --check",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "format:native": "./format_code.sh",
    "format:native:all": "./format_code.sh -a",
    "demo:init": "./deploy/demo/init-env.sh",
    "demo:up": "docker compose up --build --detach",
    "demo:down": "docker compose down",
    "demo:logs": "docker compose logs --follow",
    "demo:ps": "docker compose ps",
    "configure:native": "cmake -S native -B native/build -G Ninja -DCMAKE_BUILD_TYPE=Release -DFIBER_BUILD_TESTS=ON",
    "build:native": "cmake --build native/build --target fiber_app_access_server --parallel",
    "build:native-validator": "cmake --build native/build --target fiber_app_access_gateway_validator --parallel",
    "test:native": "cmake --build native/build --target fiber_access_server_tests --parallel && ctest --test-dir native/build --output-on-failure -L access-server",
    "test:native:sanitizers": "./native/access-server/scripts/run_sanitizers.sh all",
    "test:native:interop": "cmake -S native -B native/build-interop -G Ninja -DCMAKE_BUILD_TYPE=RelWithDebInfo -DFIBER_BUILD_TESTS=ON -DFIBER_ENABLE_LTO=OFF -DACCESS_SERVER_BUILD_EXTERNAL_INTEROP_TESTS=ON && cmake --build native/build-interop --target fiber_access_server_external_interop --parallel && ctest --test-dir native/build-interop --output-on-failure -L access-server-interop"
  },
  "devDependencies": {
    "@polycam/clang-tools": "22.1.8",
    "concurrently": "^10.0.4",
    "marked": "^18.0.9",
    "prettier": "^3.9.6"
  }
}
